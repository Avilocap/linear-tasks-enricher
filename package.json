{
  "name": "tasks-enricher",
  "version": "1.0.0",
  "description": "Automatic Linear task enrichment via Claude Code",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
